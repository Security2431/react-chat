import { connect } from 'react-redux'
import ChatPage from '../components/ChatPage'

const mapStateToProps = ctate => ({

})

const mapDispatchToProps = dispatch => ({

})

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(ChatPage)
